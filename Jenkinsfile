/*
Install Jenkins (on an EC2 instance or use a Jenkins service).

Configure Jenkins Pipeline:

Install necessary plugins: Docker, Kubernetes, AWS Credentials.
Create a new pipeline job in Jenkins.

*/